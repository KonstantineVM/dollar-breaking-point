# FWTW Reconstruction — PRE-REGISTERED FALSIFIABLE PREDICTION

**Timestamp: 2026-06-30 (written and committed BEFORE Parts 1–3 are computed).**

This file is a pre-registration. It is committed to the branch *before* the marginals are
grounded, before either interior is reconstructed, and before any F3 statistic is computed,
so that the prediction below cannot be retrofitted to the result. The test that follows is a
TEST, not a confirmation: the prediction stated here **may be REFUTED by the result**, and a
refutation is the finding to report. Do not tune toward this prediction; do not suppress a
refutation. Report whatever comes back.

## The prediction (verbatim)

> Method 1 (max-entropy, no prior) is expected to return the independence table = the
> aggregate reweighting, sign ~+0.5, no F3 loading. Method 2 (RAS with residency seed) is
> expected to return the residency interaction pattern relabeled to nationality, sign ~ the
> prior residency +0.521, no F3 loading. EITHER method returning a negative,
> correctly-signed, holder-varying F3 loading REFUTES this prediction and is the finding.

## Refutation criterion (stated before the numbers)

- **REFUTED (F3-LOADS) for a method** if that method's recovered interior yields a
  within-holder substitution sign that is **NEGATIVE, correctly signed, and holder-varying**
  (US-leg DOWN while China-nationality exposure UP). That interior loads F3, and the report
  is F3-LOADS naming the method.
- **HELD (NO-F3)** if both interiors reproduce approximately the aggregate / residency sign
  (~+0.5 / ~+0.521) with no negative holder-varying F3 loading.
- **OPEN** if the BIS IDS nationality marginal is unfetchable or an interior is incomputable.

## Anti-planting commitment

The "~+0.52 collapse" for Method 1 is a PREDICTION to be tested, **not** an input, a shortcut,
or a value carried over from any prior pass. Method 1's independence table p_ij =
(row_i · col_j)/total will be COMPUTED explicitly and its F3 statistic MEASURED from that
computed interior. Likewise Method 2's RAS interior will be obtained by actually rescaling the
DP2 residency seed to the two marginals, and its F3 statistic measured from that computed
interior. No interior's F3 loading is assumed.

## Scope

This pass STOPS after the two interiors, their F3 statistics, and the comparison to this
prediction. It does NOT rewrite DP2–DP5 and does NOT begin DP6. No date, no bare probability,
no hazard claim is produced.
